# Application resources

Place icons, images, templates, and other files shipped with the application
in this directory. Use paths relative to the package or an explicit resource
helper; do not assume the current working directory is the project root.
